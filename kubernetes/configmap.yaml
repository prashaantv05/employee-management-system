apiVersion: v1
kind: ConfigMap
metadata:
  name: ems-config
  namespace: ems
data:
  DB_HOST: "mysql-service" # Resolves through K8s DNS
  DB_NAME: "employee_db"
  DB_PORT: "3306"
  PORT: "5000"
  NODE_ENV: "production"
