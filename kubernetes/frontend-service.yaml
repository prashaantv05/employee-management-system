apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: ems
  labels:
    app: frontend
spec:
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080 # Exposes dashboard on http://<node-ip>:30080
  selector:
    app: frontend
  type: NodePort
